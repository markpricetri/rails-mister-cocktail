<%= simple_form_for [@cocktail, @dose] do |f| %>
  <%= f.input :description %>
  <%= f.input :ingredient, collection: Ingredient.all, input_html: {  class: 'multiple-select', multiple: false }, include_hidden: false %>
  <%= f.submit %>
<% end %>

